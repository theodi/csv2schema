require 'spec_helper'

describe Csv2schema do
  it 'has a version number' do
    expect(Csv2schema::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
